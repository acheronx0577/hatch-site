# API_INVENTORY

> Run `pnpm run tools:api` to regenerate a complete route listing.

Sample endpoints (non-exhaustive):

```
GET /contacts
POST /contacts
GET /contacts/{id}
PATCH /contacts/{id}
POST /messages/sms
POST /tours
GET /auth/login
GET /auth/callback
```
